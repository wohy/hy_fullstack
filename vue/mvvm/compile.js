class Compile {
  constructor(el, vm) {
    this.el = el; // 传入的 el 可能为 DOM 节点，如 document.getElementById('app')；或者是一个字符串，如'#app'
    this.vm = vm;
  }
  /**
   * 专门写一些辅助的方法
   */



  /**
   * 核心的方法
   */
}